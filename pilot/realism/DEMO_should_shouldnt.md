# Aegis — live SHOULD / SHOULD-NOT demonstration on the real kdb+ estate

Generated by `pilot/realism/demo_should_shouldnt.py`. Policy: `policy.realism.demo.json`. Data: `/home/jtoffolo/aegis-hdb/fsp1/hdb` (real, 50 partitions x 10M rows = 500M trade rows on fsp1; full estate is 4B). q: `/opt/kdb/4.1/2024.10.16/l64/q`.

## 1. SHOULD WORK — compiled and executed on the live HDB

Principal: `analyst-equities` (entitled to `sym in (AAPL;MSFT)`). HDB: `/home/jtoffolo/aegis-hdb/fsp1/hdb` — 50 partitions x 10M rows. All timings are server-side.

### a. Filtered raw select (size>500), one partition
```
request : {'table': 'trade', 'columns': ['sym', 'time', 'price', 'size'], 'filters': [{'col': 'size', 'op': '>', 'value': 500}], 'date': {'from': '2025.06.01', 'to': '2025.06.01'}}
compiled: 1000000 sublist (select sym, time, price, size from trade where date=2025.06.01, size>500, sym in `AAPL`MSFT, i<1000000)
executed: 186,827 rows in 261 ms
BOUND   : distinct sym in result = `s#,`AAPL  (entitlement held)
```
-> PASS

### b. Aggregation: avg price + total size by sym, one partition
```
request : {'table': 'trade', 'aggs': [{'fn': 'avg', 'col': 'price', 'as': 'avg_px'}, {'fn': 'sum', 'col': 'size', 'as': 'tot_sz'}], 'by': ['sym'], 'date': {'from': '2025.06.01', 'to': '2025.06.01'}}
compiled: 1000000 sublist (select avg_px:avg price, tot_sz:sum size by sym from trade where date=2025.06.01, sym in `AAPL`MSFT)
executed: 2 rows in 3 ms
PROOF   : matches independent uncapped reference query = True
          ref: select avg_px:avg price, tot_sz:sum size by sym from trade where date=2025.06.01, sym in (`AAPL;`MSFT)
```
-> PASS

### c. Top-N: 10 highest-price trades, sort desc + limit, one partition
```
request : {'table': 'trade', 'columns': ['sym', 'time', 'price'], 'sort': {'col': 'price', 'dir': 'desc'}, 'limit': 10, 'date': {'from': '2025.06.01', 'to': '2025.06.01'}}
compiled: 10 sublist `price xdesc (select sym, time, price from trade where date=2025.06.01, sym in `AAPL`MSFT, i<1000000)
executed: 10 rows in 18 ms
BOUND   : returned exactly 10 rows (limit honoured); syms = `s#,`AAPL
```
-> PASS

### d. VWAP (size-weighted price) by sym, one partition
```
request : {'table': 'trade', 'aggs': [{'fn': 'wavg', 'col': 'price', 'weight': 'size', 'as': 'vwap'}], 'by': ['sym'], 'date': {'from': '2025.06.01', 'to': '2025.06.01'}}
compiled: 1000000 sublist (select vwap:size wavg price by sym from trade where date=2025.06.01, sym in `AAPL`MSFT)
executed: 2 rows in 5 ms
PROOF   : matches independent uncapped reference query = True
          ref: select vwap:size wavg price by sym from trade where date=2025.06.01, sym in (`AAPL;`MSFT)
```
-> PASS

### e. As-of join trade|>quote on (sym,time); effective spread, one partition
```
request : {'join': {'type': 'asof', 'on': ['sym', 'time'], 'left': {'table': 'trade', 'columns': ['sym', 'time', 'price'], 'date': {'from': '2025.06.01', 'to': '2025.06.01'}}, 'right': {'table': 'quote', 'columns': ['sym', 'time', 'bid', 'ask'], 'date': {'from': '2025.06.01', 'to': '2025.06.01'}}, 'select': [{'as': 'sym', 'expr': {'col': 'sym'}}, {'as': 'eff_spread', 'expr': {'op': 'sub', 'args': [{'col': 'price'}, {'op': 'div', 'args': [{'op': 'add', 'args': [{'col': 'bid'}, {'col': 'ask'}]}, {'lit': 2}]}]}}]}}
compiled: select sym:sym, eff_spread:(price-((bid+ask)%2)) from aj[`sym`time; 1000000 sublist (select sym, time, price from trade where date=2025.06.01, sym in `AAPL`MSFT, i<1000000); 1000000 sublist (select sym, time, bid, ask from quote where date=2025.06.01, sym in `AAPL`MSFT, i<1000000)]
executed: 196,640 rows in 44363 ms
BOUND   : joined result syms = `s#,`AAPL  (both sides entitlement-filtered)
NOTE    : the aj over ~187K x ~187K rows is genuinely heavy — the elapsed
          time above is the real cost on this data, shown honestly.
```
-> PASS

## 2. SHOULD NOT WORK — rejected by the compiler, never reach kdb+

Each prints the exact rejection reason. No q is emitted, nothing executes.

### a. Table not on the allowlist
```
request : {'table': 'positions', 'columns': ['sym'], 'date': {'from': '2025.06.01', 'to': '2025.06.01'}}
principal: analyst-equities
REJECTED: table 'positions' not on the query allowlist
```
-> BLOCKED

### b. Column not on the allowlist (trade.secret)
```
request : {'table': 'trade', 'columns': ['sym', 'secret'], 'date': {'from': '2025.06.01', 'to': '2025.06.01'}}
principal: analyst-equities
REJECTED: column 'secret' not on the allowlist for this table
```
-> BLOCKED

### c. Missing date on a partitioned table
```
request : {'table': 'trade', 'columns': ['sym', 'price']}
principal: analyst-equities
REJECTED: table 'trade' is partitioned — a date range is required
```
-> BLOCKED

### d. Raw range over the span cap (20 days > max 5)
```
request : {'table': 'trade', 'columns': ['sym', 'price'], 'date': {'from': '2025.06.01', 'to': '2025.06.20'}}
principal: analyst-equities
REJECTED: raw row-listing date range spans 20 days (max 5) — narrow the range or use an aggregation
```
-> BLOCKED

### e. Injection in a filter VALUE: AAPL`;system"id"
```
request : {'table': 'trade', 'columns': ['sym'], 'date': {'from': '2025.06.01', 'to': '2025.06.01'}, 'filters': [{'col': 'sym', 'op': '=', 'value': 'AAPL`;system"id"'}]}
principal: analyst-equities
REJECTED: unsafe string value: 'AAPL`;system"id"'
```
-> BLOCKED

### f. Injection in a COLUMN name: sym; delete trade
```
request : {'table': 'trade', 'columns': ['sym; delete trade'], 'date': {'from': '2025.06.01', 'to': '2025.06.01'}}
principal: analyst-equities
REJECTED: invalid column identifier: 'sym; delete trade'
```
-> BLOCKED

### g. op:meta by an UN-entitled principal (analyst-nobody)
```
request : {'table': 'trade', 'op': 'meta'}
principal: analyst-nobody
REJECTED: principal 'analyst-nobody' has no row entitlements (default-deny) - denied
```
-> BLOCKED

## 3. ENTITLEMENT CANNOT BE WIDENED — proven on real data

`analyst-equities` is entitled to `sym in (AAPL;MSFT)` only. It explicitly asks for GOOG and NVDA (which DO exist in the data). The compiled q carries BOTH the agent's own filter AND the mandatory entitlement filter — their intersection is empty, so it returns 0 rows. The analyst cannot widen.

```
request : {'table': 'trade', 'columns': ['sym', 'price'], 'date': {'from': '2025.06.01', 'to': '2025.06.01'}, 'filters': [{'col': 'sym', 'op': 'in', 'value': ['GOOG', 'NVDA']}]}
compiled: 1000000 sublist (select sym, price from trade where date=2025.06.01, sym in `GOOG`NVDA, sym in `AAPL`MSFT, i<1000000)
          ^ note BOTH `sym in (`GOOG;`NVDA)` (agent) AND `sym in (`AAPL;`MSFT)` (mandatory)
executed: 0 rows in 13 ms
control : `AAPL`GOOG`NVDA present in data? = 111b  (GOOG/NVDA exist; 0 rows is the entitlement, not a typo)
```
-> PASS — entitlement held (0 rows)

## 4. INJECTION DEFENCE (IFC) — deterministic, content-independent

FIDES-style information-flow control. A privileged sink (`submit_order`) is vetoed when its arguments are derived from UNTRUSTED content (e.g. an injected instruction in a tool result), regardless of wording. Trusted-derived actions pass. Labels JOIN monotonically — mixing trusted + untrusted is still untrusted.

### a. submit_order derived from the OPERATOR's own (trusted) instruction
```
derived from: ['operator_instruction']
joined label: integrity=TRUSTED, confidentiality=PUBLIC
IFC verdict : ALLOW   (expected ALLOW)
```
-> PASS

### b. submit_order derived from a POISONED tool result (untrusted)
```
derived from: ['poisoned_quote_feed']
joined label: integrity=UNTRUSTED, confidentiality=PUBLIC
IFC verdict : BLOCK (ifc.untrusted_to_privileged)   (expected BLOCK)
reason      : untrusted-tainted input cannot drive privileged tool 'submit_order' (possible prompt injection via tool output)
```
-> PASS

### c. submit_order derived from TRUSTED + UNTRUSTED mixed (join -> untrusted)
```
derived from: ['operator_instruction', 'poisoned_quote_feed']
joined label: integrity=UNTRUSTED, confidentiality=PUBLIC
IFC verdict : BLOCK (ifc.untrusted_to_privileged)   (expected BLOCK)
reason      : untrusted-tainted input cannot drive privileged tool 'submit_order' (possible prompt injection via tool output)
```
-> PASS

## Headline

- **§1 should-work:** 5/5 allowed queries ran bounded on real data (correct vs uncapped references / entitlement held).
- **§2 should-not:** 7/7 unsafe requests rejected by the compiler — none reached kdb+.
- **§3 entitlement:** out-of-scope request returned 0 rows (cannot be widened, proven on real data).
- **§4 IFC:** 3/3 injection cases correct (trusted ALLOW, untrusted/mixed BLOCK).
