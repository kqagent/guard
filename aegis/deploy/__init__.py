"""Deployment subpackage: confinement runners, the signed-bundle builder, and
the reference hardened deploy artifacts (k8s.yaml, Dockerfile, compose).

Importable: `aegis.deploy.landlock_confine`, `aegis.deploy.build_bundle`.
Data files (k8s.yaml, deployment-profile.json, *.sh) ship with the wheel.
"""
